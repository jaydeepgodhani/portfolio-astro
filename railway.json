{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'Skipping build, using pre-built dist'"
  },
  "deploy": {
    "startCommand": "npx serve dist -l $PORT -s --cache-control"
  }
}