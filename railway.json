{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'Skipping build, using pre-built dist'"
  },
  "deploy": {
    "startCommand": "npm serve dist -l $PORT"
  }
}