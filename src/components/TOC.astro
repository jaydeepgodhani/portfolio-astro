---
const { headings } = Astro.props;
---

{
  headings.map((h: any) => {
    if (h.depth === 1) {
      return <ul>{h}</ul>;
    } else if (h.depth === 2) {
      return <ul class="pl-4">{h}</ul>;
    }
  })
}
